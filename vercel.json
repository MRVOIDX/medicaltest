{
  "buildCommand": "npm run build && npx esbuild server/vercel-app.ts --platform=node --packages=external --bundle --format=esm --outfile=api/_handler.mjs",
  "outputDirectory": "dist/public",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
